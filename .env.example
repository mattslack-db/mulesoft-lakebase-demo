# Copy this file to .env and fill in your real value.
# .env is git-ignored and must NEVER be committed.
#
# Databricks OAuth client-credentials secret.
# Created in Databricks → Settings → Service Principals → <your SP> → OAuth secrets.
DATABRICKS_CLIENT_SECRET=your-databricks-oauth-client-secret-here
